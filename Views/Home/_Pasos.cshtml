<label>
    <i class="bi bi-check-square"></i>
    Pasos de la tarea
</label>

<button class="btn btn-secondary btn-sm" onclick="manejarClickAgregarPaso()">
    <i class="bi bi-plus"></i>
</button>

<div data-bind="foreach: pasos">
    <div class="mt-2">
        <div class="row">
            <div class="col-1">
                <input type="checkbox" class="form-check-input" name="chbPaso" data-bind="{checked: realizado}" />
            </div>
            <div class="col-11">
                @* span. contenedor del texto *@
                @* lo esconceremos si estamos en modo edicion, con hidden *@
                <span data-bind="text: descripcion, hidden: modoEdicion"></span>
                @* en caso de estar en modo edicion mostraremos un texarea *@
                <textarea name="txtPasoDescripcion" class="form-control" placeholder="Agrege una descripción"
                data-bind="{value: descripcion, visible: modoEdicion}"></textarea>
            </div>
        </div>
    </div>
</div>